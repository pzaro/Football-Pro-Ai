{
  "name": "apex-omega-background-engine",
  "version": "6.0.0",
  "private": true,
  "description": "APEX OMEGA v6.0 background service, headless runtime and local dashboard",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check background-bridge.js && node --check stats.js"
  },
  "dependencies": {
    "playwright-core": "^1.55.0"
  },
  "engines": {
    "node": ">=22.5.0"
  }
}
